import { UserCart } from "../../types";

const CartProduct: React.FC<UserCart> = props => {
    const {name, salePrice, description, category, } = props.product

  return <>

  </>;
};

export default CartProduct;
